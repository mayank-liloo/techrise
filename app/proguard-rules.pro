# Add project specific ProGuard rules here.
# By default, the active rules are defined in the SDK's proguard-android-optimize.txt.
# You can add custom rules for Hilt and Retrofit parsing if necessary.

-keep class com.techrise.di.** { *; }
-keepattributes Signature, InnerClasses, EnclosingMethod
