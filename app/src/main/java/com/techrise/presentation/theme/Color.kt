package com.techrise.presentation.theme

import androidx.compose.ui.graphics.Color

// Sporty Dark Theme Color Palette (Aligned background with top bar, and lighter cards)
val SportyBackground = Color(0xFF131E31)      // Dark navy background (same as previous Tech Rise header)
val SportySurface = Color(0xFF24334B)         // Lighter navy-blue surface (complaint box)
val SportySurfaceVariant = Color(0xFF2E3E5B)  // Elevated variant

val SportyPrimary = Color(0xFF2B95F5)         // Energetic Sky Blue
val SportyOnPrimary = Color(0xFFFFFFFF)
val SportyPrimaryContainer = Color(0xFF16549E)// Deep Blue Accent
val SportyOnPrimaryContainer = Color(0xFFD0E4FF)

val SportySecondary = Color(0xFFE53E3E)       // Vibrant Flag Red
val SportyOnSecondary = Color(0xFFFFFFFF)

val SportyTextPrimary = Color(0xFFF8FAFC)     // Slate 50
val SportyTextSecondary = Color(0xFF94A3B8)   // Slate 400
val SportyBorder = Color(0xFF2E3E5B)          // Outline border

// Light Theme Color Palette (White, Yellow, and Orange Accent)
val CleanSlateBackground = Color(0xFFFAFAF9) // Warm Off-White Background
val SurfaceLight = Color(0xFFFFF5EC)         // Soft Peach-White (Slight Orange) Surface
val SurfaceVariantLight = Color(0xFFFFEEDB)  // Deeper Soft Orange Surface Variant

val CoolBlue = Color(0xFFFF7A00)             // Energetic Orange (Primary)
val CoolBlueVariant = Color(0xFFE65100)      // Deep Orange Variant
val TextPrimaryLight = Color(0xFF1F2937)     // Dark charcoal text for high contrast
val TextSecondaryLight = Color(0xFF4B5563)   // Medium gray text for sub-titles
val LightBorder = Color(0xFFFEF08A)          // Soft Yellow Border

// Status & Priority Colors mapped to light theme
val StatusPending = Color(0xFFDC2626)         // Clean red
val StatusActive = Color(0xFFD97706)          // Amber/Orange
val StatusResolved = Color(0xFF059669)        // Emerald Green

val PriorityHigh = Color(0xFFDC2626)
val PriorityMedium = Color(0xFFD97706)
val PriorityLow = Color(0xFF6B7280)
